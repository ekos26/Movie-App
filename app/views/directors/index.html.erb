<h1>🍿All Directors</h1>
<% @directors.each do |director| %>
<%= link_to director.name, director_path(director) %>
<br>
<% end %>

<%= button_to "Add Director", new_director_path(@director), :method => :get %>
